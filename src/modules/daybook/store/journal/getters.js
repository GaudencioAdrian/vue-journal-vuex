//Los getters no son asincronos y traen informacion del state que podemos procesarlo
//Toman una funcion que es el state
/* export const myGetters = (state) => {
    // se hace un return de lo que se necesita del state
    return state;
 }
*/
//Obtener todas las entradas para buscar el termino de busqueda cuando la persona busca algo quiero que regrese todas las entradas quecoincidan con el termino

export const getEntriesByTerm = (/*state*/) => {
    // se hace un return de lo que se necesita del state
 }

 //Solo traer una entrada basada en el id
 export const getEntryById = (/*state*/) => {

 }

 /*****Getters ES UNA FORMA RAPIDA DE LEER EL STATE Y TRAER LA INFORMACION COMO SE NECESITA*/
